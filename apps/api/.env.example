DATABASE_URL=postgresql://user:password@localhost:5432/letreco
CORS_ORIGIN=http://localhost:3003

# JWT compartilhado com gleider-dev para autenticar chamadas admin.
# Deve ser a mesma string nos dois repos. Rotação exige update e redeploy de ambos.
API_JWT_SECRET=replace-with-strong-random-string-shared-with-gleider-dev
